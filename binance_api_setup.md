# Binance API 安全設定指南

目標：用 2000 USDT 實盤交易（每幣 500 USDT），其他資產完全隔離不受影響。

---

## 步驟

### 1. 建立子帳戶

登入幣安 → 右上角頭像 → **帳戶** → **子帳戶管理** → 建立子帳戶

> 如果沒看到子帳戶選項，需要先完成 KYC 身份驗證。

### 2. 轉入 2000 USDT

主帳戶 → **錢包** → **資金劃轉** → 選「主帳戶 → 子帳戶」→ 轉入 2000 USDT

子帳戶裡就只有這 2000 USDT，其他資產（BTC、ETH、主帳戶的任何東西）物理隔離，API 完全碰不到。

### 3. 在子帳戶建立 API Key

登入**子帳戶**（或在主帳戶的子帳戶管理頁操作）→ API 管理 → 建立 API Key

權限設定：

| 權限 | 設定 |
|------|------|
| 讀取 | ✅ 開啟 |
| 現貨交易 | ✅ 開啟 |
| 提幣 | ❌ 絕對關閉 |

**IP 白名單**填入 Lightsail IP：`34.206.128.225`

### 4. 把 API Key 填入系統設定

Settings 頁 → Binance API 金鑰 → 貼上子帳戶的 Key & Secret → 儲存

### 5. 風控設定（保險閥）

Settings → 風控設定：
- **每日最大虧損**：設 `100`（單日虧超過 100 USDT 自動停止所有策略）
- **單筆最大倉位**：設 `500`（每筆不超過 500 USDT）

---

## 資金架構

```
主帳戶（BTC、ETH、其他資產 — API 完全無法存取）
    ↓ 只轉 2000 USDT
子帳戶（只有 2000 USDT）
    ↓ API Key（只開現貨交易，綁 IP，無提幣）
系統（每幣 500 USDT，日損上限 100 USDT）
```

最壞情況：子帳戶的 2000 USDT 歸零。主帳戶的所有資產完全不受影響。

---

## 策略建立

進系統 → 策略管理 → **一鍵實盤** → 選 Crypto Pulse，每筆 500 USDT，選 BTC / ETH / SOL / BNB → 建立
